% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_dea.R
\name{dea_seq_indirect}
\alias{dea_seq_indirect}
\title{Sequential DEA (Input-Oriented, Tecnologia Secuencial Indirecta)}
\usage{
dea_seq_indirect(
  data,
  id_col,
  time_col,
  input_cols,
  output_cols,
  dmu_id,
  period,
  na_rm = FALSE,
  allow_negative = FALSE
)
}
\arguments{
\item{data}{\code{data.frame} o \code{tibble} con el panel completo.}

\item{id_col}{Nombre de la columna que identifica la DMU.}

\item{time_col}{Nombre de la columna con el tiempo o periodo.}

\item{input_cols}{Vector de columnas que representan inputs (>= 1).}

\item{output_cols}{Vector de columnas que representan outputs (>= 1).}

\item{dmu_id}{Identificador de la DMU objetivo.}

\item{period}{Periodo de evaluacion; se consideran referencias con tiempo <= \code{period}.}

\item{na_rm}{Si es \code{TRUE}, elimina observaciones de referencia con NA (la DMU
objetivo debe estar completa). Si es \code{FALSE}, lanza error ante cualquier NA.}

\item{allow_negative}{Indica si se permiten valores negativos en inputs/outputs.
Por defecto (\code{FALSE}) se lanza error si se detectan valores < 0.}
}
\value{
Lista con \code{theta}, \code{lambdas}, \code{status}, \code{status_message}, \code{x_opt},
\code{y_opt}, \code{reference_data} (panel usado) y \code{target} (registro de la DMU objetivo).
}
\description{
Construye la tecnologia secuencial (usa todas las observaciones con
\code{time_col <= period}) y resuelve el modelo DEA CCR input-oriented asociado.
Es el bloque basico utilizado para calcular indices Malmquist-Luenberger,
aunque el indice completo no esta implementado en este paquete.
}
\examples{
panel <- data.frame(
  id = c("A", "A", "B", "B"),
  year = c(1, 2, 1, 2),
  x1 = c(5, 4, 6, 5),
  y1 = c(10, 11, 9, 10)
)

dea_seq_indirect(
  data = panel,
  id_col = "id",
  time_col = "year",
  input_cols = "x1",
  output_cols = "y1",
  dmu_id = "A",
  period = 2
)

}
\seealso{
\code{\link[=solve_dea_input]{solve_dea_input()}}
}
